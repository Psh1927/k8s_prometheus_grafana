# Prometheus & Grafana Demo application deployment to Kubernetes

Deploy this application to Minikube or any other cluster:
```
kubectl apply -f .
```

Grafana open on `http://localhost:3000/` 
Prometheus open on `http://localhost:9090/` 